
@model EcdlBooking.Models.School; 

@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}



<div class="container">

    <h3>
        Scuola : @Model.Name
    </h3>

    <h3>
        Indirizzo : @Model.Address
    </h3>
    <h3>
        Città : @Model.City
    </h3>

    <br />

    <a type="button" class="btn btn-danger"
       asp-controller="School"
       asp-action="AddClass"
       asp-route-id="@Model.Id">+</a>

    <p3> Classi</p3>
    <br>
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th scope="col">Classe</th>
                <th scope="col"></th>
            </tr>

        </thead>
        <tbody>
            <!---Stampa delle classi assegante-->
            @*
            @if(Model.Classes != null) {
                 @foreach (var i in Model.Classes )
                {
                <tr>
                    <th scope="col">@i.Name</th>
                    <th scope="col"></th>
                </tr>

                    }
            }
            *@
            <!--     sperimentazione     -->

            <tr>
                <th scope="col"> nome classe</th>
                <th>
                    <button type="submit" class="btn btn-primary">Submit</button>

                </th>

            </tr>



        </tbody>
    </table>



    <!--
        <a type="button" class="btn btn-danger"
           asp-controller="/school"
           asp-action="AddClass"
        asp-route-id="id"
           >+</a>
    -->








</div>